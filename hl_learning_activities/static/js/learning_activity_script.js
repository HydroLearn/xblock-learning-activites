function Learning_Activity_Studio(runtime, xblock_element) {

    // extend from the default HLCK5 studio object
    HLCK5_XBlockStudio.call(this, runtime, xblock_element);


    $(function($) {
        console.log('Initialized custom LA script.')

    })
}
